<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.VidMax.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">#00000000</item>
        <item name="android:navigationBarColor">#00000000</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Launcher splash (androidx.core:core-splashscreen, same approach as mpvRex).
         Shows logo on black from the very first frame on ALL Android versions,
         then hands over to Theme.VidMax.NoActionBar automatically. -->
    <style name="Theme.VidMax.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_screen_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="windowSplashScreenAnimationDuration">800</item>
        <item name="postSplashScreenTheme">@style/Theme.VidMax.NoActionBar</item>
    </style>
</resources>
